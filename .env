# ================================================================
# 공통 설정 — 모든 환경에서 사용
# 환경별 차이는 .env.local / .env.dev 에서 override
# ================================================================

# RDS
RDS_HOST=dev-shared-rds.cdaao0ik8058.ap-northeast-2.rds.amazonaws.com
RDS_PORT=3306
RDS_USER=admin
RDS_PASSWORD=thswhdgus1!
RDS_DB=keyword_collector

# Fetcher
DEFAULT_CRAWL_DELAY_MS=1000
DEFAULT_RENDER_MODE=static
PROXY_PROVIDER=direct

# Sink
SINK_TYPE=file
FILE_SINK_DIR=./data
LOG_DIR=./logs

# Solr (SINK_TYPE=solr 일 때만 필요)
# SOLR_URL=http://localhost:8983/solr/news
# SOLR_BATCH_SIZE=100

# Retry / Backoff
MAX_ATTEMPTS=5
BACKOFF_BASE_SECONDS=30
BACKOFF_MAX_SECONDS=3600
CLAIM_TIMEOUT_SECONDS=300
DISCOVERY_403_RESCHEDULE_SEC=1800 # 30분 — 403 후 같은 키워드 재시도 유예

# Rules cache
RULES_CACHE_TTL_SECONDS=60

# Logging
LOG_LEVEL=INFO
LOG_ROTATION=daily
HEARTBEAT_INTERVAL_SECONDS=60

# 포털별 발견 최대 페이지 수 (키워드 1회 실행당)
NAVER_MAX_PAGES=30
DAUM_MAX_PAGES=30
GOOGLE_MAX_PAGES=5
BAIDU_MAX_PAGES=5
NAVER_STOCK_MAX_PAGES=30
